node-linker=hoisted
# Make brace-expansion / minimatch visible to React Native codegen (Gradle node process)
# so the CJS-safe pin is not buried in an isolated store path.
public-hoist-pattern[]=*brace-expansion*
public-hoist-pattern[]=*minimatch*
shamefully-hoist=true
